# 🚀 Jellyfin Analytics Dashboard - Configuration
# This file contains default values that work out of the box
# No changes needed for basic deployment!

# Server Configuration
PORT=3000
NODE_ENV=production

# Database (automatically created)
DATABASE_URL="file:/app/data/analytics.db"

# Security: Encryption key for sensitive settings
# For production, generate a secure key: openssl rand -base64 32
ENCRYPTION_KEY=JellyfinAnalytics2024SecureDefaultKey!ChangeInProduction

# 🎯 Configure your Jellyfin server via the web interface:
# 1. Start the application: docker compose up
# 2. Open: http://localhost:3000
# 3. Go to Settings tab
# 4. Enter your Jellyfin URL and API key
# 5. Test connection and save

# Legacy environment variables (use web UI instead):
# JELLYFIN_URL=http://localhost:8096
# JELLYFIN_API_KEY=your_jellyfin_api_key_here
